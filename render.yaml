services:
  - type: web
    name: ai-persona-chat
    env: python
    buildCommand: pip install -r backend/requirements.txt && cd frontend && npm install && npm run build && cp -r dist ../backend/static
    startCommand: cd backend && python -m uvicorn app:app --host 0.0.0.0 --port $PORT 2>&1
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: persona-chat-db
          property: connectionString
      - key: AI_API_KEY
        sync: false
      - key: AI_API_BASE
        sync: false
      - key: AI_MODEL
        value: gpt-4o

databases:
  - name: persona-chat-db
    plan: free
